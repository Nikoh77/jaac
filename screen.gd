extends RichTextLabel

func _ready():
#	var font: Font = self.get_theme_default_font()
#	var sizez = self.get_theme_default_font_size()
#	prints(font, sizez)
#	set_scroll_follow(true)
#   set_process(true)
#   set_process_input(true)
	pass
	
func _input(event):
#	if event is InputEventKey and !event.is_echo() and event.is_pressed():
#		var keySymbol = PackedByteArray([event.get_unicode()])
#		if event.get_keycode() == 16777222 || event.get_keycode() == 16777221:
#			Telnet.client.put_utf8_string(str("\r"))
#		elif event.get_keycode() > 16777349 and event.get_keycode() < 16777360:
#			Telnet.client.put_utf8_string(str(event.scancode - 16777350))
##		elif (event.get_keycode_with_modifiers().is_shift_pressed()):
##			Telnet.client.put_utf8_string(str(keySymbol))
#		else:
#			prints(keySymbol, OS.get_keycode_string(event.get_key_label_with_modifiers()))
#			Telnet.send(keySymbol)

